# Бриф для Claude Design — блок «Варианты ремонта» (лукбук Владен)

> Скопируй весь этот текст в Claude Design как промпт. Бриф самодостаточный:
> в нём весь контент, токены бренда и поведение адаптива. Картинки уже лежат
> в проекте (пути ниже) — их пересоздавать не нужно, только подставить.

---

## Задача

Пересобрать блок «Варианты ремонта» на сайте **vladen-crimea.ru** (Next.js 15, App
Router, Tailwind, TypeScript). Сейчас блок свёрстан как **фиксированный холст 1920×1080
и масштабируется через `transform: scale()`** — поэтому ведёт себя как вставленная
картинка: на ПК мелко и с пустотой по краям, на телефоне обрезается.

**Нужно:** заменить на нормальный адаптивный блок в потоке страницы (НЕ `position:
absolute`, НЕ фиксированный холст, НЕ `transform: scale`). Всё на `%`, `fr`, `clamp()`,
`minmax()` — чтобы тянулось и сжималось под любую ширину, ничего не обрезалось.

## Главное поведение

Один интерактивный блок с тремя раскрываемыми уровнями (Эконом / Комфорт / Премиум).

- **ПК (≥768px):** ряд из трёх табов-кнопок с названием и ценой. Клик по табу →
  ниже плавно (CSS height/opacity, Framer Motion) показывается панель уровня:
  1) галерея 4 комнат адаптивной сеткой `repeat(auto-fit, minmax(220px, 1fr))`,
  2) таблица «Что входит в стоимость»,
  3) карточка «Общий стиль» с чипами.
  По умолчанию открыт Эконом. Активный таб подсвечен оранжевым.
- **Телефон (<768px):** те же три уровня — вертикальным **аккордеоном**. Тап по
  заголовку «Эконом» → раскрывается его контент (комнаты в 1–2 колонки, таблица,
  стиль), остальные свёрнуты. Одновременно открыт один уровень. Стрелка-шеврон
  поворачивается при раскрытии.

Над блоком — короткий интро-заголовок (есть ниже). Под блоком — строка ключевых
цифр (стартовая цена / срок / гарантия / стаж) и две кнопки: «Скачать PDF»
(`/buklet.pdf`) и «Смотреть проекты» (скролл к секции проектов).

## Токены бренда (взять точно эти)

- Шрифт: **Manrope**, `system-ui, sans-serif`. Заголовки `font-weight: 800`,
  плотный трекинг `letter-spacing: -0.02em…-0.04em`.
- Цвета:
  - Чернила (тёмный фон / текст): `#1A1A1A`, второй тёмный `#161616`, `#1F1F1F`
  - Бумага (светлый фон / текст): `#F5F3EF`
  - Акцент (оранжевый): `#F39C2D`
  - Тёплый фон уровня «Комфорт»: `#EBE5D8`, рендер-подложка `#FBF8F1`
  - Линии: `rgba(26,26,26,0.14)` на светлом, `rgba(245,243,239,0.16)` на тёмном
- Фоны уровней: **Эконом** — светлый `#F5F3EF`; **Комфорт** — тёплый бежевый
  `#EBE5D8`; **Премиум** — тёмный `#161616` (текст становится `#F5F3EF`).
- Микро-лейблы: `text-transform: uppercase`, `letter-spacing: 0.18–0.24em`,
  `font-size: 11–13px`, `font-weight: 700`, `opacity: 0.55`.
- Маркеры списков и акценты цен — оранжевые квадраты/текст `#F39C2D`.
- Адаптив заголовков обязателен в три ступени (правило проекта):
  `text-3xl sm:text-4xl md:text-5xl` — ступень `sm` не пропускать.

## Интро над блоком

- Надзаголовок (eyebrow): `— ВАРИАНТЫ РЕМОНТА` (оранжевая чёрточка + текст).
- H2: «Три уровня отделки **под ключ в Крыму**» («под ключ в Крыму» — оранжевым).
- Подзаголовок: «Три уровня отделки со стартовыми ценами от 17 000 ₽ за квадратный
  метр — от инвестиционной аренды до архитектурного премиума. Подобранные
  материалы, прозрачная смета, готовые сценарии заселения.»

## Контент уровней

### Уровень 01 — Эконом · от 17 000 ₽/м²
- Подзаголовок уровня: «Под аренду и инвестиции»
- Для кого (чипы): **Под аренду** (активный/solid), Быстрое заселение,
  Ограниченный бюджет, Инвестиционная недвижимость
- Комнаты (4 фото): Гостиная, Кухня, Санузел, Спальня
- Что входит в стоимость:
  - Пол — Ламинат 32 класс
  - Стены — Покраска / обои
  - Потолок — Натяжной потолок
  - Освещение — Накладные светильники
  - Санузел — Базовая сантехника, обычный унитаз
  - Двери — Стандартные межкомнатные
- Общий стиль — заголовок: «Чисто, современно, практично.»
  текст: «Аккуратный современный ремонт без визуального перегруза. Подходит для
  аренды, перепродажи и быстрого запуска квартиры — без дизайнерских претензий,
  но с уважением к деталям.»
  чипы: Минимум декора · Светлая палитра · Быстрый запуск
- Фон уровня: светлый `#F5F3EF`

### Уровень 02 — Комфорт · от 27 000 ₽/м²
- Подзаголовок уровня: «Для собственного проживания»
- Для кого (чипы): **Для семьи** (активный), Для жизни, Баланс цены и дизайна,
  Комфортное проживание
- Комнаты (4 фото): Гостиная, Кухня, Санузел, Спальня
- Что входит в стоимость:
  - Пол — Кварцвинил
  - Стены — Декоративная штукатурка / покраска
  - Потолок — Натяжной со скрытой подсветкой
  - Освещение — Встроенные светильники, локальный свет
  - Санузел — Современная сантехника, скрытые подводки
  - Двери — Скрытые или улучшенные двери
- Общий стиль — заголовок: «Современно, уютно, для себя.»
  текст: «Ремонт «для себя». Интересные текстуры, скрытая подсветка, инсталляция
  в санузле, более качественная кухня. Уютный современный интерьер с акцентом на
  сценарии повседневной жизни.»
  чипы: Скрытый свет · Кварцвинил · Декор. штукатурка
- Фон уровня: тёплый бежевый `#EBE5D8`

### Уровень 03 — Премиум · от 37 000 ₽/м²
- Подзаголовок уровня: «Архитектурный современный интерьер»
- Для кого (чипы): **Статусный интерьер** (активный), Дорогая недвижимость,
  Максимальный визуал, Дизайнерский подход
- Комнаты (4 фото): Гостиная, Кухня, Санузел, Спальня
- Что входит в стоимость:
  - Пол — Крупноформатный керамогранит / инженерная доска
  - Стены — Декоративные панели, камень, сложные текстуры
  - Потолок — Теневой профиль
  - Освещение — Архитектурный свет, много скрытой подсветки
  - Санузел — Премиальная сантехника, инсталляции
  - Двери — Скрытые двери premium
- Общий стиль — заголовок: «Архитектура, свет и деталь.»
  текст: «Дорогие материалы, скрытый свет, продуманная архитектура. Премиум
  отличается не «цветом», а уровнем деталей: теневые потолки, крупный
  керамогранит, дизайнерская мебель и встроенные решения.»
  чипы: Теневой профиль · Архитектурный свет
- Фон уровня: тёмный `#161616` (на тёмном фоне текст и линии светлые)

## Строка цифр под блоком

- от **17 000** ₽ / м² — Стартовая цена
- от **30** дней — Срок ремонта
- **2** года — Гарантия
- с **2014** — Работаем в Крыму

(числа — оранжевые `#F39C2D`, подписи — мелкий uppercase-лейбл)

## Картинки (уже в проекте, подставить как есть)

Лежат в `public/buklet/`, формат 4:3, `object-fit: cover`:

- Эконом: `econ-living.png`, `econ-kitchen.png`, `econ-bath.png`, `econ-bedroom.png`
- Комфорт: `comf-living.png`, `comf-kitchen.png`, `comf-bath.png`, `comf-bedroom.png`
- Премиум: `prem-living.png`, `prem-kitchen.png`, `prem-bath.png`, `prem-bedroom.png`

Порядок комнат: Гостиная → Кухня → Санузел → Спальня.

## Технические требования

- Next.js 15 App Router, `"use client"` (есть интерактив), TypeScript, Tailwind.
- Никаких фиксированных `width: 1920` / `height: 1080` и `transform: scale()`.
- Сетки: `grid-template-columns: repeat(auto-fit, minmax(...))` и `clamp()` для
  типографики, чтобы тянулось плавно.
- Брейкпоинты проекта: 375 / 640 / 768 / 1280. Проверить на 375px (мобайл),
  768px (планшет/таб), 1280px+ (десктоп).
- Анимация раскрытия — простая (Framer Motion `AnimatePresence` или CSS grid-rows
  `0fr → 1fr`). Не усложнять.
- Доступность: табы/аккордеон с клавиатуры (Tab, Enter/Space), `aria-expanded`,
  `aria-controls`, фокус-стили.
- Изображения через `next/image` или `<img loading="lazy">`.
- Картинки комнат не обрезать важное: `aspect-ratio: 4/3`, `object-fit: cover`.

## Замена в коде

Новый компонент кладётся в `components/sections/` и заменяет текущий
`BukletSlider.tsx` (он рендерится на главной между Hero и секцией проектов,
`nextSectionId="project-case"`). Сохранить пропс перехода к следующей секции и
ссылку на скачивание `/buklet.pdf`.
