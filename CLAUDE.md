# Владен — строительная компания, Крым

## Статус проекта
Прод — свой VPS (НЕ Vercel): reg.ru, `80.78.246.196`, ssh-алиас `prod-main`, APP_DIR `/var/www/vladen`, pm2 app `vladen` + nginx, домен vladen-crimea.ru.
**Прод крутится с ветки `lookbook-accordion`** (не master). Деплой: на сервере `git pull origin lookbook-accordion` → `bash deploy/redeploy.sh`.
Старый Selectel `89.111.168.203` (`prod-vladen-old`) упал за неоплату — не использовать.
Playwright MCP подключён (headless) — можешь открывать сайт и смотреть.

## Правила работы
- Обсуждай концепцию ПЕРЕД написанием кода
- Перед большими изменениями: `git checkout -b имя-ветки`
- Не усложняй анимации — простой CSS/JS лучше сложного SVG
- Секции контента всегда в нормальном потоке, НЕ absolute
- После рабочего этапа: коммит + пуш

## Данные компании
- Телефон: +7 (978) 717-44-47
- Email: vladen2026@mail.ru
- Офис: г. Симферополь, ул. Киевская 41, офис 727
- ИНН: 2317074414 | ООО «ВЛАДЕН»
- Услуги: подбор участка, проектирование, дизайн, инженерные сети, фундамент и стены, кровля, дизайнерские ремонты (дома до 3 этажей)

## Стек
Next.js 15 App Router + Tailwind CSS + TypeScript + Framer Motion + Lenis (smooth scroll)

## Ключевые принципы
- Mobile first: 375px → 768px → 1280px+
- Токены и ключи только в `.env.local`, не коммитить
- SEO: метатеги + openGraph на каждой странице, sitemap.xml, robots.txt
- Форма отправляет на `/api/telegram` (имя + телефон, проверка origin, только POST)
- LoadingContext (`contexts/LoadingContext.tsx`) — CountUp ждёт пока лоадер не уйдёт

## АДАПТИВНОСТЬ — КРАСНАЯ ЛИНИЯ
Каждый новый компонент обязан проверяться на 375px, 640px, 1280px.
- Заголовки ВСЕГДА через три ступени: `text-3xl sm:text-4xl md:text-5xl` — пропускать sm нельзя
- `flex-shrink-0` на фиксированных элементах хедера (лого, бургер), иначе прыгает на узких экранах
- Паддинги карточек: `p-6 sm:p-8` — не `p-8` без мобильного варианта
- `gap` в flex/grid на мобиле уменьшать: `gap-6 sm:gap-10`
- Текст внутри хедера скрывать на xs если занимает место: `hidden sm:block`

## SEO-фокус
60% ремонт квартир/домов в Симферополе, 40% строительство в Крыму.

## Порядок секций на главной
Hero → Services → ProjectCase → WhyWe → Calculator → Contacts → Partners → Reviews
