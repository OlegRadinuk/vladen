# Контекст проекта: Владен (строительная компания, Крым)

## Статус
Сайт сделан и работает. Стабильная версия на `master`: https://github.com/OlegRadinuk/vladen.git
Следующий этап: двухколоночный layout (левая панель 30% анимация + правая 70% контент) + слайдер кейсов.

## Правила работы с кодом
- Обсуждай концепцию ПЕРЕД написанием кода
- Перед большими изменениями: `git checkout -b имя-ветки`
- Не усложняй анимации — простые CSS/JS лучше сложного SVG
- Секции контента всегда в нормальном потоке, НЕ absolute
- После рабочего этапа: `git add . && git commit && git push`

## Реальные данные компании
- Телефон: +7 (978) 717-44-47 | Email: vladen2026@mail.ru
- Офис: г. Симферополь, ул. Киевская 41, офис 727
- ИНН: 2317074414 | ООО «ВЛАДЕН»
- Услуги: подбор участка, проектирование, дизайн, инженерные сети, фундамент и стены, кровля, дизайнерские ремонты (дома до 3 этажей)
- Логотип: `public/logo.png`

Ты помогаешь разрабатывать сайт на Next.js 15 + Tailwind + TypeScript.

## Общие принципы

- **Mobile first** — сначала верстка под 375px, потом планшет (768px), потом десктоп (1280px+)
- **Адаптивность** — все отступы через `px-4 sm:px-6 lg:px-8`, контейнеры через `max-w-7xl mx-auto`
- **Безопасность** — все токены и ключи в `.env.local`, не коммитить `.env` нужно глобавльная безопассность на всем пути проекта
- **SEO** — метатеги на каждой странице, sitemap.xml, robots.txt

## Цветовая система пока что примерная, может быть нужно будет менять корректировать

| Роль | HEX |
|------|-----|
| Тёмный фон (Hero, кейсы) | `#2A2F35` |
| Светлый фон (основной) | `#ECF0F1` |
| Карточки на светлом | `#FFFFFF` |
| Акцент (кнопки, цифры, линии) | `#D97706` |
| Текст на тёмном | `#D1D5DB` |
| Текст на светлом | `#2C3E50` |
| Текст второстепенный | `#7F8C8D` |

## Шрифты

- Заголовки: Oswald (через Google Fonts)
- Текст: Inter (через Google Fonts)

## Структура проекта (Next.js App Router)

```
app/
├── layout.tsx        # корневой layout (Header + Footer)
├── page.tsx          # главная
├── services/
│   └── page.tsx
├── projects/
│   └── page.tsx
├── about/
│   └── page.tsx
├── contacts/
│   └── page.tsx
└── api/
    └── telegram/
        └── route.ts  # отправка заявок

components/
├── ui/
│   ├── Container.tsx
│   ├── Button.tsx
│   └── Card.tsx
├── layout/
│   ├── Header.tsx
│   └── Footer.tsx
└── sections/
    ├── Hero.tsx
    ├── Services.tsx
    ├── ProjectCase.tsx
    ├── WhyWe.tsx
    ├── Calculator.tsx
    └── Contacts.tsx
```

## Стили и эффекты

- **Hover** — без прыжков, только glow, смена цвета границы, появление оранжевого акцента
- **Анимации появления** — при скролле (выезжание слева/справа), но без фанатизма
- **Параллакс** — только в Hero (движение фона за мышью)
- **Карточки услуг** — фото вместо иконок, при наведении оранжевая полоска снизу

## Форма и Telegram

- Форма: имя + телефон
- Отправка на `/api/telegram`
- Валидация телефона (минимальная)
- Защита: проверка origin, только POST

## SEO

- На каждой странице: `title`, `description`, `openGraph`
- `sitemap.xml` и `robots.txt` в public

## Что нужно сделать в первую очередь (MVP)

1. Корневой layout (Header + Footer)
2. Главная страница (Hero, Услуги, Кейс, Почему мы, Калькулятор, Контакты)
3. Страницы: Услуги, Проекты, О компании, Контакты
4. Форма + Telegram
5. SEO база

## Запуск

После создания проекта: `npm run dev`
Сайт доступен на `http://localhost:3000`
